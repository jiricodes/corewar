#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <sys/file.h>
#include <sys/types.h>

int32_t		decode(const uint8_t *source, size_t size)
{
	int32_t result;
	uint8_t sign;
	
}
int main(int argc, char **argv)
{
	int fd;
	uint8_t buffer;

	if (argc != 2)
		exit();
	fd = open(argv[1], O_RDONLY)
	if (fd >= 0)
	{

	}
}